export function ChartPlaceholder() {
  return (
    <section className="flex h-96 w-full flex-col justify-center rounded-lg border border-zinc-800 bg-zinc-950 p-6 text-center">
      <span className="text-sm text-zinc-500">차트 라이브러리가 연결되면 이 영역에 실시간 캔들 차트가 표시됩니다.</span>
    </section>
  );
}
